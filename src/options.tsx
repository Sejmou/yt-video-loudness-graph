import React from "react";
import ReactDOM from "react-dom";

const Options = () => {

  return (
    <>
      Options will show up here if necessary.
    </>
  );
};

ReactDOM.render(
  <React.StrictMode>
    <Options />
  </React.StrictMode>,
  document.getElementById("root")
);
